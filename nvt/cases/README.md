# NVT Cases

Per-case working metadata belongs here.

Use this area for case indexes, source references, annotation notes, and temporary structured metadata that support Ground Truth creation.

Hard regression truth belongs in `nvt/ground_truth/` only after review/lock.
